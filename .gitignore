# IDE-specific files
.idea/*

# Python files we don't care about
__pycache__/*
src/__pycache__/*
*.pyc
.venv/

# macOS files we don't care about
*.DS_Store

# User-specific files aren't stored in Git.
# (although we are storing the alias file, which is intended to be user-specific at some point)
usr/*.html
usr/*.db
usr/*.db-journal

# log files
app.log*

# Ignore exported HTML files for now
html/export*

# Thinking about containerizing the app, but it's a WIP.
# Desktop GUIs don't seem like they are made for containers.
# The initial benefit I saw of containerizing is to run the app inside
# a macOS image. But then how would the user sign into their Notes app
# within a container? I'm not sure
Dockerfile